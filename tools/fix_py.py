with open("apply_carousel.py", "r") as f:
    text = f.read()

import json
# Re-import JSON to insert safely
# We know the object from index 10 onward since data logic is at top. Wait, better to just edit python script logic.
